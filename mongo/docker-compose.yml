version: '3.1'

services:

  mongo:
    image: mongo
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: tomeee
      MONGO_INITDB_ROOT_PASSWORD: pass
      MONGO_INITDB_DATABASE: offers
    volumes:
      - ./init-mongo.js:/docker-entrypoint-initdb.d/init-mongo.js:ro
    ports:
      - "27017:27017"
    networks:
      - express-mongo

  mongo-express:
    image: mongo-express:0.44
    restart: always
    ports:
      - "8081:8081"
    environment:
      ME_CONFIG_MONGODB_ADMINUSERNAME: tomeee
      ME_CONFIG_MONGODB_ADMINPASSWORD: pass
    networks:
      - express-mongo

networks:
  express-mongo: